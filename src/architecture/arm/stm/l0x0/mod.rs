use crate::{TargetSequence, TargetSequenceTraits};

pub struct L0x0 {
    // All we need to know about generic L0x0
}

impl L0x0 {
    pub fn identify_target<'a>(self) -> Option<(TargetSequence, TargetSequenceTraits<'a>)> {
        todo!()
    }
}
